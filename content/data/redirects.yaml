routes:
  /:
    template: home
    data: page.home

collections:
  /blog/:
    permalink: /blog/{slug}/
    template: index
    filter: tag:-hash-hidden

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
